using UnityEngine;
using System.Collections;

public class Background : MonoBehaviour {

    //private int w, h;
    //private float fixWidth = 1280.0f;
    //private float fixHeight = 800.0f;

	void Start () {
                // 画面右上のワールド座標をビューポートから取得
                //Vector2 max = Camera.main.ViewportToWorldPoint (new Vector2 (1, 1));
                // スケールを求める。
                //Vector2 scale = max * 2;
                //w = Screen.width;
                //h = Screen.height;
                //Vector2 scale = new Vector2(w/fixWidth, h/fixHeight);
                // スケールを変更。
                //transform.localScale = scale;
	}
	
	void Update () {
	
	}
}
